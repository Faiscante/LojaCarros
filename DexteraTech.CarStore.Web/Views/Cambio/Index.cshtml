@model List<CambioViewModel>
@{
    ViewData["Title"] = "Cambio";
    ViewData["SubTitle"] = "Listagem";
    ViewData["AddUrlAction"] = Url.Action("Editar", "Cambio");
}

<body>
    <div class="text-center">

        <div class="row">
            <!-- column -->
            <div class="col-sm-12 align-self-center">
                @if (Model.Any())
                {

                    <div class="table-responsive">
                        <table class="table user-table">
                            <thead>
                                <tr>
                                    <th scope="col">Nome</th>
                                    <th scope="col"></th>
                                </tr>
                            </thead>
                            <tbody>

                                @foreach (CambioViewModel cambio in Model)
                                {
                                    <tr>
                                        <td>@cambio.NmCambio</td>
                                        <td>
                                            <div class="input-group justify-content-md-center">
                                                <div class="btn-group btn-group-sm float-end" role="group">
                                                    <a role="button" class="btn btn-primary" asp-route-IdCambio="@cambio.IdCambio" asp-controller="Cambio" asp-action="Editar" data-bs-toggle="tooltip" data-bs-placement="top" data-bs- data-bs-title="Alterar">
                                                        <i class="mdi mdi-grease-pencil"></i>
                                                    </a>
                                                    <a role="button" class="btn btn-danger" onclick="CambioIndexView.excluirCambioConfirm($(this))" data-idcambio="@cambio.IdCambio" data-bs-toggle="tooltip" data-bs-placement="top" data-bs-title="Excluir">
                                                        <i class="mdi mdi-delete-forever"></i>
                                                    </a>
                                                </div>
                                            </div>
                                        </td>

                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                }
                else
                {
                    <div class="alert alert-warning" role="alert">
                        Nenhuma cambio cadastrado!
                    </div>
                }
            </div>
        </div>

    </div>

    @section Scripts {
        <script src="~/js/views/Cambio/Index.js"></script>
        <script>
            CambioIndexView.urlExcluirCambio = '@Url.Action("ExcluirCambio")';
        </script>
    }

</body>